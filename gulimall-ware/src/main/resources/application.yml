spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://192.168.125.131:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml #默认就可以找到 自己写的去掉classpath*中的*
  global-config:
    db-config:
      id-type: auto

server:
  port: 1100